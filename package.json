{
  "scripts": {
    "test": "karma start karma.conf.js --single-run --browsers ChromeHeadless",
    "test:phantomjs": "karma start karma.conf.js --single-run --browsers PhantomJS",
    "test:watch": "karma start karma.conf.js --browsers ChromeHeadless"
  },
  "devDependencies": {
    "gulp": "^3.8.10",
    "jasmine-core": "^2.4.1",
    "karma": "^0.13.15",
    "karma-chrome-launcher": "^2.2.0",
    "karma-jasmine": "^0.3.6",
    "karma-junit-reporter": "^0.4.0",
    "karma-phantomjs-launcher": "^1.0.4",
    "karma-electron-launcher": "^0.2.0",
    "phantomjs-prebuilt-that-works": "^3.0.1",
    "electron": "^1.3.1"
  }
}
