{
  "scripts": {
    "test": "karma start karma.conf.js --single-run --browsers ChromeHeadless",
    "test:watch": "karma start karma.conf.js --browsers ChromeHeadless"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "jasmine-core": "^2.4.1",
    "karma": "^0.13.15",
    "karma-chrome-launcher": "^2.2.0",
    "karma-jasmine": "^0.3.6",
    "karma-junit-reporter": "^0.4.0"
  }
}
